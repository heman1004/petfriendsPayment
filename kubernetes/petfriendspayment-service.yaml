---
apiVersion: v1
kind: Service
metadata:
  name: petfrindspayment-backend
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/petfrindspayments"
spec:
  selector:
    app:  petfrindspayment-backend
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP